class Solution:
    def findDisappearedNumbers(self, nums: List[int]) -> List[int]:
        final = []
        a= len(nums)
        b= set(nums)
        for i in range(1,a+1):
            if i not in b:
                final.append(i)
        return final 
       
